# WriteMelo 隐私说明

更新日期：2026 年 7 月 30 日

## 本地处理

WriteMelo 将文档、版本快照、设置、同意选择和个人词典保存在设备上。本地补全、拼写、语法规则、大纲和检查清单不会把写作内容发送给 WriteMelo 或 AI 供应商。

浏览器版使用 IndexedDB 和本地存储；Windows 应用由 Electron 将浏览器配置保存在当前 Windows 用户的应用数据目录。

## 可选 AI

AI 默认关闭。关闭期间，WriteMelo 不会发起 AI API 请求，也不会产生 AI 供应商费用；本地补全、拼写、诊断、大纲和版本历史仍然可用。

启用“仅询问 AI”不会发送文档。用户实际提问时，只发送该问题和完成请求所需的最少设置。附带当前全文需要单独勾选；启用后，每次 AI 提问都会同时发送当前文档，直到用户关闭该选项。任何实际 AI 请求都可能消耗供应商额度或依据其条款产生费用，全文请求通常会使用更多 token。

当前 beta 已实现同意边界。正式 AI 供应商的接口、留存条款、区域路由和账号控制完成配置与审查前，不应启用生产 AI。

## 自备 AI 服务

Windows 应用通过 Electron `safeStorage` 加密 API Key，并将密文保存在当前 Windows 用户的应用数据目录；渲染界面不会收到已经保存的明文 Key。请求由 Electron 主进程直接发送到用户选择的接口。

远程供应商会收到完成已同意请求所必需的文本，并可能依据自身条款留存或收费。WriteMelo 不保证供应商始终提供免费额度。Ollama 只能使用 localhost 上的 HTTP 接口，其他供应商接口必须使用 HTTPS。

## 账号与诊断

本地写作功能不要求账号。托管账号和工单服务启用后，可能处理邮箱、验证事件、额度用量、购买记录和工单内容。生产密钥、供应商资源池、反滥用阈值、用户写作内容和生产日志不保存在公开仓库中。

## 联系方式

隐私或安全问题请通过仓库的私密安全报告渠道提交。
